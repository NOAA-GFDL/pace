name: "Main unit tests"
on:
  push:
  pull_request:
      types: [opened, synchronize, reopened, ready_for_review, labeled, unlabeled]

jobs:
  main_unit_tests:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/mlee03/pace:test
    steps:
        - name: checkout
          uses: actions/checkout@v4
          with:
            submodules: 'recursive'
        - name: generate input files
          run: |
            chmod +x /home/test_scripts
            mkdir /_w/pace/pace/tests/main/input
            test_scripts/create_eta79.py && mv eta79.nc /_w/pace/pace/tests/main/input
            test_scripts/create_eta91.py && mv eta91.nc /_w//pace/pace/tests/main/input
        - name: setup environment and run
          run: |
            source /root/.bashrc
            pip3 install --upgrade pip setuptools wheel
            pip3 install -r requirements_dev.txt -c constraints.txt	    
            pytest -x tests/main
